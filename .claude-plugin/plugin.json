{
  "name": "claude-drift",
  "description": "Measure whether a new model actually behaves differently on your own Claude Code sessions. /drift scan reads the session logs already on disk and costs nothing; /drift replay re-runs sampled turns with the new model and with the old one, so the report separates real drift from replay noise. Ships a same-model control because a raw 'the model changed' number mostly measures the replay setup.",
  "version": "0.2.0",
  "author": {
    "name": "Youngmin Ko",
    "url": "https://github.com/mandu5"
  },
  "homepage": "https://github.com/mandu5/claude-drift",
  "repository": "https://github.com/mandu5/claude-drift",
  "license": "MIT",
  "keywords": [
    "claude-code",
    "model-drift",
    "evaluation",
    "replay",
    "regression",
    "sessions",
    "noise-band",
    "bootstrap"
  ]
}
